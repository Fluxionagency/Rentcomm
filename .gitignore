# Real credentials — copy api/config.sample.php to api/config.php on the server.
public_html/api/config.php

# Uploaded marketing assets (managed via the Admin Portal on the server)
public_html/downloads/*
!public_html/downloads/.htaccess
!public_html/downloads/README.md

# Vercel CLI local project link + any local env files (real values live in
# the Vercel dashboard's Environment Variables, never in the repo)
.vercel
.env
.env.local
